{
  "name": "j8-app",
  "version": "0.2.0",
  "description": "J8 prototype — web app with login, client records, protocol builder, quotes, booking, and orders. Zero external dependencies — runs on Node.js alone.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
